```php
<?php

    /**
     * Function receives an array with integer numbers,
     * should return its sum. It is not alowed to use built-in php functions.
     *
     * Функція отримує на вхід масив чисел, що має повернути суму цих чисел.
     * Не використовувати вбудовані функції підсумовування php, наприклад array_sum.
     *
     * @param array $arr
     * @return integer
     */
    function my_sum(array $arr): int
    {
        //...wite your code here
    }


    /**
     * Function receives a long string with many words.
     * It should return the same string, but words,
     * larger then 6 symbols, should be changed, symbols
     * after the sixth one should be replaced by symbol *
     *
     * Функція отримує на вхід довгий рядок з безліччю слів.
     * Вона повинна повернути той же рядок, але в словах, які довші за 6 символів,
     * функція повинна замість всіх символів після шостого поставити одну зірочку.
     * Приклад: Зі слова 'verwijdering' має вийти 'verwij*'
     *
     * @param string $shortMe
     * @return string
     */
    function shortener(string $shortMe): string 
    {
        //...wite your code here
    }

    /**
     * Function receives an array of strings.
     * Please return number of strings, which
     * length is at least 2 symbols and first character
     * is equal to the last character
     *
     * Функція отримує на вхід масив рядків. Повернути треба кількість рядків,
     * які не коротші двох символів і у яких перший та останній
     * Символ збігаються.
     *
     * @param array $arr
     * @return integer
     */
    function compare_ends(array $arr): int 
    {
        //...wite your code here
    }



    /**
     * Function receives a string, should return this string reversed.
     *
     * Функція отримує на вхід рядок, повинна повернути її перевернутою.
     * Використовувати стандартні функції типу strrev не можна
     *
     * @param string $str
     * @return string
     */
    function reverse_string(string $str): string 
    {
        //...write your code here
    }
```
